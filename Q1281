class Solution {
public:
    int subtractProductAndSum(int n) {
     
        int sum=0,product=1,r;
       while(n!=0){
            r=n%10;
            sum=sum+r;
            product=product*r;
            n=n/10;
        }
        int result=product-sum;
        return result;
    } 
  
};
